import styled from "styled-components";

export const Form = styled.form`
  h4 {
    margin: 1rem 0;
    color: #fcd83c;
    font-weight: bold;
  }

  .MuiFormControl-root {
    margin-bottom: 1rem;
  }

  .MuiButton-root {
    background-color: #252f49;
  }
`;
